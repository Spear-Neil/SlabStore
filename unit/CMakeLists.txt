function(add_unit name files)
    add_executable(unit-${name} ${files})
    message(STATUS "add unit-${name}")
endfunction()

aux_source_directory(./ unit_test_files)
foreach (file ${unit_test_files})
    get_filename_component(name ${file} NAME_WE)
    add_unit(${name} ${file})
endforeach ()
